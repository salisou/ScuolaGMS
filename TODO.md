# TODO List for Completing Services, Mappings, Controllers, and Program Registration

## ✅ 1. Update MappingProfile.cs

- Add AutoMapper mappings for all entities (Entity to Dto, CreateDto to Entity, UpdateDto to Entity) for Corso, Docente, Lezione, Presenza, Valutazione, Iscrizione, Classe, Voto.

## ✅ 2. Complete CorsoService.cs

- Implement CRUD methods (GetAllAsync, GetByIdAsync, CreateAsync, UpdateAsync, DeleteAsync) following the pattern of AulaService/StudenteService.

## ✅ 3. Complete DocenteService.cs

- Implement CRUD methods (GetAllAsync, GetByIdAsync, CreateAsync, UpdateAsync, DeleteAsync) following the pattern of AulaService/StudenteService.

## ✅ 4. Complete LezioneService.cs

- Implement CRUD methods (GetAllAsync, GetByIdAsync, CreateAsync, UpdateAsync, DeleteAsync) following the pattern of AulaService/StudenteService.

## ✅ 5. Complete PresenzaService.cs

- Implement CRUD methods (GetAllAsync, GetByIdAsync, CreateAsync, UpdateAsync, DeleteAsync) following the pattern of AulaService/StudenteService.

## ✅ 6. Complete ValutazioneService.cs

- Implement CRUD methods (GetAllAsync, GetByIdAsync, CreateAsync, UpdateAsync, DeleteAsync) following the pattern of AulaService/StudenteService.

## ✅ 7. Complete IscrizioneService.cs

- Implement CRUD methods (GetAllAsync, GetByIdAsync, CreateAsync, UpdateAsync, DeleteAsync) following the pattern of AulaService/StudenteService.

## ✅ 8. Complete ClasseService.cs

- Implement CRUD methods (GetAllAsync, GetByIdAsync, CreateAsync, UpdateAsync, DeleteAsync) following the pattern of AulaService/StudenteService.

## ✅ 9. Complete VotoService.cs

- Implement CRUD methods (GetAllAsync, GetByIdAsync, CreateAsync, UpdateAsync, DeleteAsync) following the pattern of AulaService/StudenteService.

## ✅ 10. Complete AulaController.cs

- Implement CRUD actions (GetAll, GetById, Create, Update, Delete) following the pattern of StudenteController.

## ✅ 11. Complete ClasseController.cs

- Implement CRUD actions (GetAll, GetById, Create, Update, Delete) following the pattern of StudenteController.

## ✅ 12. Complete CorsoController.cs

- Implement CRUD actions (GetAll, GetById, Create, Update, Delete) following the pattern of StudenteController.

## ✅ 13. Create DocenteController.cs

- Create new controller file and implement CRUD actions (GetAll, GetById, Create, Update, Delete) following the pattern of StudenteController.

## ✅ 14. Create LezioneController.cs

- Create new controller file and implement CRUD actions (GetAll, GetById, Create, Update, Delete) following the pattern of StudenteController.

## ✅ 15. Create PresenzaController.cs

- Create new controller file and implement CRUD actions (GetAll, GetById, Create, Update, Delete) following the pattern of StudenteController.

## ✅ 16. Create ValutazioneController.cs

- Create new controller file and implement CRUD actions (GetAll, GetById, Create, Update, Delete) following the pattern of StudenteController.

## ✅ 17. Create IscrizioneController.cs

- Create new controller file and implement CRUD actions (GetAll, GetById, Create, Update, Delete) following the pattern of StudenteController.

## ✅ 18. Create VotoController.cs

- Create new controller file and implement CRUD actions (GetAll, GetById, Create, Update, Delete) following the pattern of StudenteController.

## ✅ 19. Update Program.cs

- Register all services in DI (add Scoped registrations for CorsoService, DocenteService, LezioneService, PresenzaService, ValutazioneService, IscrizioneService, ClasseService, VotoService).

## 20. Test the API endpoints

- Run the application and test all endpoints to ensure they work correctly.
